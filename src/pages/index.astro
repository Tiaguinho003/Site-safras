---
/**
 * Home PT-BR (rota raiz /). Renderiza HomePage com locale automático
 * via Astro.currentLocale (= "pt-br" por padrão).
 */
import HomePage from "../components/pages/HomePage.astro";
---

<HomePage />
